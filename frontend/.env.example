# Copy to .env.local and adjust if the API is not on the default port.
# The frontend talks to the backend only over HTTP through this URL; there is no
# other coupling between the two.
VITE_API_BASE_URL=http://localhost:5057
